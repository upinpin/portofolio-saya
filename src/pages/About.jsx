import React from 'react';
import Tentang from '../components/Tentang';

export default function About({ darkMode, fadeInUpVariants }) {
  return <Tentang darkMode={darkMode} fadeInUpVariants={fadeInUpVariants} />;
}
