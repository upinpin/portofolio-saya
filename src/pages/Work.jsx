import React from 'react';
import Proyek from '../components/Proyek';

export default function Work({ darkMode, fadeInUpVariants }) {
  return <Proyek darkMode={darkMode} fadeInUpVariants={fadeInUpVariants} />;
}
