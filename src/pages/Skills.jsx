import React from 'react';
import Keahlian from '../components/Keahlian';

export default function Skills({ darkMode, fadeInUpVariants }) {
  return <Keahlian darkMode={darkMode} fadeInUpVariants={fadeInUpVariants} />;
}
